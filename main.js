const valueToDisplay = document.querySelector("#text1");
console.log(valueToDisplay.textContent)

const buttons = document.querySelectorAll(".button");
buttons.forEach(button => {
        button.addEventListener('click', (e)=>{
            
            
        })
})
